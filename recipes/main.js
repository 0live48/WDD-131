// main.js

import { getRecipes } from "./recipes.mjs";

// Placeholder function for rendering the list of recipes
// We will only render one for now, based on the prompt's instruction to choose any recipe (Apple Crisp).
function init() {
  // In a real application, you'd use getRecipes() to fetch data
  // and then dynamically generate the HTML. For this initial setup,
  // we're focusing on static HTML and CSS/Responsiveness.
  console.log("Recipe Book Initialized.");
}

init();